{
  "name": "codenotch-win",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --test tests/task-model.test.mjs tests/media-format.test.mjs tests/pill-modules.test.mjs",
    "test:ui": "playwright test",
    "tauri": "tauri"
  },
  "dependencies": {
    "@hugeicons/core-free-icons": "^4.3.2",
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@tauri-apps/cli": "^2",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
